spring.application.name=attendance-management
# Database connection (Neon PostgreSQL — overridable via env vars)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-late-dust-ahakze1q-pooler.c-3.us-east-1.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_fvej2nGimo6t}

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Server port
server.port=${PORT:8080}

# Admin registration key
admin.registration.key=${ADMIN_REGISTRATION_KEY:ADMIN12345}
